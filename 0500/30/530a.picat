main =>
    A = read_int(),
    B = read_int(),
    C = read_int(),
    D = B * B - 4 * A * C,
    if D = 0 then
        write(-B / (2 * A))
    else
        X1 = (-B + sqrt(D)) / (2 * A),
        X2 = (-B - sqrt(D)) / (2 * A),
        println(min(X1, X2)),
        println(max(X1, X2))
    end
.
